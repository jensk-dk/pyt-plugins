[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "myapp-plugin-sdk"
version = "0.1.0"
description = "SDK for developing myapp plugins"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
include = ["myapp_plugin_sdk*"]
